name: lint

on:
  push:
  pull_request:

jobs:
  line-length:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Check line lengths
        run: python3 scripts/LintLineLength.py
